# AI Studio Browser Manager 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改

# (必需) 所有浏览器实例将要访问的统一目标 URL
# 这是 AI Studio Preview 页面的 URL
CAMOUFOX_INSTANCE_URL="https://aistudio.google.com/apps/drive/1CPW7FpWGsDZzkaYgYOyXQ_6FWgxieLmL"

# (可选) HTTP 服务器端口
# 默认: 7860
# PORT=7860

# (可选) 服务器监听地址
# 默认: 0.0.0.0
# HOST=0.0.0.0

# (可选) 浏览器运行模式
# 设置为 'false' 以启用有头模式（显示浏览器窗口）
# 默认: true (无头模式)
# CAMOUFOX_HEADLESS=false

# (可选) 设置代理服务器
# 格式: http://proxy.example.com:8080
# 默认: 未设置
# CAMOUFOX_PROXY="http://proxy.example.com:8080"

# (可选) 每个浏览器实例启动之间的延迟时间（秒）
# 默认: 30
# INSTANCE_START_DELAY=30

# (可选) 认证源配置
# 方式 1: 使用环境变量（推荐）
# AUTH_JSON_1='{"cookies": [...]}'
# AUTH_JSON_2='{"cookies": [...]}'

# 方式 2: 使用 auth/ 目录下的文件
# 将认证文件放在 auth/ 目录下，命名为 auth-1.json, auth-2.json 等